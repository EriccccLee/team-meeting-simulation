# team-meeting-simulation
#
# 핵심 의존성: 없음 (표준 라이브러리만 사용)
# LLM 호출은 claude -p subprocess 를 통해 처리합니다.

# PDF 첨부 파일 지원 (이미 설치됨)
pypdf>=4.0.0

# 선택적 — 추후 AutoGen GroupChat 기능 추가 시:
# autogen-agentchat>=0.4.0

# 웹 프론트엔드 (FastAPI + SSE)
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# 파일 포맷 변환
python-docx>=1.0.0
tabulate>=0.9.0
python-pptx>=1.0.0

# Slack skill extraction
slack-sdk>=3.27.0
python-dotenv>=1.0.0
unidecode>=1.3.0
